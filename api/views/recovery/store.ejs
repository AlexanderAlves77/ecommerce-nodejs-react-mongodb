<!DOCTYPE html>
<html>
  <head>
    <% include(../partials/header.ejs) %>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <div class="container">
      <div class="row">
        <div class="col-sm-3 col-sm-offset-3">
          <h2>Recuperar Senha</h2>
          <hr />
          <br />

          <% if(success) { %>
          <div class="alert alert-success"><%= success %></div>
          <div class="text-center">Agora pode fazer seu login novamente :)</div>
          <% } %> <% if(error) { %>
          <div class="alert alert-danger"><%= error %></div>
          <% } %> <% if(!success) { %>
          <p>Digite aqui sua nova senha.</p>

          <form action="/v1/api/usuarios/senha-recuperada" method="post">
            <input type="hidden" name="token" value="<%= token %>" />
            <div class="form-group">
              <input
                type="password"
                name="password"
                class="form-control"
                placeholder="Digite sua nova senha"
              />
            </div>
            <button class="btn btn-primary">Criar Nova Senha</button>
          </form>
          <% } %>
        </div>
      </div>
    </div>
  </body>
</html>
